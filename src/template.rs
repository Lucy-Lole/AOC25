use anyhow::Result;

pub fn run_day_six(file_as_string: String) -> Result<String> {
    let x = process_data(file_as_string);

    let result_value = "A";

    return Ok(format!("Result: {result_value}"));
}

fn process_data(file: String) -> (()) {

    return ();
}